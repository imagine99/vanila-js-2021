const btn_advice=document.querySelector(".top-bar__advice");
const advice=btn_advice.querySelector(p);
function handleMouse()
{
    const num=Math.random()*3
    if(num===0)
    {
        inner
    }
}
btn_advice.addEventListener("mouseenter",handleMouse)